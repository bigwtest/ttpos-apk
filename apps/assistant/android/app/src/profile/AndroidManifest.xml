<manifest 
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
>
    
    <application 
        tools:replace="android:icon"
        android:icon="@mipmap/launcher_icon"
        android:label="TTPOS Go"
        android:enableOnBackInvokedCallback="true"
        android:allowBackup="false"
        android:fullBackupContent="false"
    >

        <activity 
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"
        >

            <!-- 
                指定在Android进程启动时立即应用于此Activity的Android主题。
                 当Flutter UI初始化时，用户可以看到此主题。
                 之后，此主题继续决定Flutter UI背后的窗口背景。
            -->
            <meta-data 
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"
            />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 
            请勿删除下面的元数据。
            这些数据被Flutter工具用来生成GeneratedPluginRegistrant.java
        -->
        <meta-data 
            android:name="flutterEmbedding"
            android:value="2"
        />
    </application>

    <!-- 
        查询可以处理文本的活动所需的权限，参见：
        https://developer.android.com/training/package-visibility 和
        https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT
        特别是，这被Flutter引擎在 io.flutter.plugin.text.ProcessTextPlugin 中使用。 
    -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>

    <queries>
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
    </queries>

    <uses-feature 
        android:name="android.hardware.telephony"
        android:required="false"
    />
    <uses-feature 
        android:name="android.hardware.camera"
        android:required="false"
    />
    <uses-feature 
        android:name="android.hardware.usb.host"
        android:required="false"
    />

    <!-- 
        互联网权限不影响 `permission_handler` 插件，
        但如果您的应用需要访问互联网，则需要此权限。
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- `contacts`（联系人）权限组 -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />

    <!-- `storage`（存储）权限组 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- Android 12及以下版本的存储读取权限 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <!-- 
        Android 13及更新版本的精细媒体权限。
        更多信息请参见：https://developer.android.com/about/versions/13/behavior-changes-13#granular-media-permissions
    -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <!-- `camera`（相机）权限组 -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- `sms`（短信）权限组 -->
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_WAP_PUSH" />
    <uses-permission android:name="android.permission.RECEIVE_MMS" />

    <!-- `phone`（电话）权限组 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.ADD_VOICEMAIL" />
    <uses-permission android:name="android.permission.USE_SIP" />
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <uses-permission android:name="android.permission.WRITE_CALL_LOG" />
    <uses-permission android:name="android.permission.BIND_CALL_REDIRECTION_SERVICE" />

    <!-- `calendar`（日历）权限组 -->
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />

    <!-- `location`（位置）权限组 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- `microphone`（麦克风）或 `speech`（语音）权限组 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- `sensors`（传感器）权限组 -->
    <uses-permission android:name="android.permission.BODY_SENSORS" />
    <uses-permission android:name="android.permission.BODY_SENSORS_BACKGROUND" />

    <!-- `accessMediaLocation`（访问媒体位置）权限组 -->
    <uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION" />

    <!-- `activityRecognition`（活动识别）权限组 -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />

    <!-- `ignoreBatteryOptimizations`（忽略电池优化）权限组 -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- `nearby devices`（附近设备）权限组 -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES" />

    <!-- `manage external storage`（管理外部存储）权限组 -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <!-- `system alert windows`（系统提示窗口）权限组 -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- `request install packages`（请求安装包）权限组 -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- `access notification policy`（访问通知策略）权限组 -->
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />

    <!-- `notification`（通知）权限组 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- `alarm`（闹钟）权限组 -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
</manifest>