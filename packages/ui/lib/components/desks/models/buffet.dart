import 'package:flutter/material.dart';

class BuffetOption {
  final bool isRemainBuffetCountdownColor;
  final Color buffetCountdownLeft10minColor;
  final Color buffetCountdownLeft20minColor;

  BuffetOption({
    required this.isRemainBuffetCountdownColor,
    required this.buffetCountdownLeft10minColor,
    required this.buffetCountdownLeft20minColor,
  });
}
